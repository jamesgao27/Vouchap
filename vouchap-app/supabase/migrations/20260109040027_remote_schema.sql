-- Baseline row recorded on remote as name "remote_schema" (e.g. after `supabase db pull`).
-- statements was null in supabase_migrations.schema_migrations; schema already matches remote at time of pull.
-- Intentionally no DDL so re-apply is a no-op if this file is ever run on a fresh DB after this version.
SELECT 1;
