# Copy to .env for local dev (never commit .env)

EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_anon_key

# AI provider (optional): gemini | deepseek — must match Supabase AI_PROVIDER_DEFAULT
# EXPO_PUBLIC_AI_PROVIDER=deepseek

# Gemini: GEMINI_API_KEY in Supabase Function secrets only.
# DeepSeek: DEEPSEEK_API_KEY + AI_PROVIDER_DEFAULT=deepseek
# Deploy: supabase functions deploy gemini-proxy
