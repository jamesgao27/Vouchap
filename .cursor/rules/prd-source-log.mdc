---
description: Log all substantive user prompts (including non-PRD) and pre/post code implementation notes to docs/PRD-SOURCE-LOG.md.
alwaysApply: true
---

# PRD / 工程对话溯源日志（必遵，全量）

## 单一事实来源

- 文件路径：**`docs/PRD-SOURCE-LOG.md`**（Vouchap 仓库 `docs/`）。
- 作用：保存  
  1）**用户提示词**（**含与 PRD 无关**的一切有信息量的请求、追问、更正）；  
  2）助手在本轮中的说明，**必须覆盖**（若存在）**实现代码前**与**实现代码后**两段：改代码前要交代意图/假设/拟动路径；改代码后要交代实际改动、如何验证、残留风险。  
  涉 PRD 的需求仍可另汇总进 `docs/PRD-Vouchap-Platform-And-Marketing-Site.md`，**不替代**本文件的「全量链条」定位。

## 何时必须追加

在**完成本轮用户可见的交付**（回答、改代码、跑命令后的结论等）之后，**编辑 `docs/PRD-SOURCE-LOG.md`**，在「## 记录」下按文件内模板**追加**一个 `###` 条目，**除非**仅属于下面「可跳过」。

- **未改代码**：`Assistant（实现后说明）` 写清结论、推理或操作结果即可；`实现前` 可写「无代码变更」或从略。  
- **改了代码**：**必须**同时填写（可简写但不悬空的）**实现前**（计划/范围）与**实现后**（摘要 + 验证建议）。

## 何时可跳过（极少数）

- 用户仅「收到 / 谢谢 / OK」等**无新需求**的收束语，且助手无可追加的实质说明；  
- 完全重复的追问已由**同一条日志条目**覆盖（可改为在原条目补一句，而非新开 `###`）。

**若无法判断是否跳过，必须追加**（可标注「低信息 / 简短回合」）。

## 每条记录至少包含

1. **User**：原话要点或忠实摘要（长则「节选」并保留关键句）。  
2. **Assistant（实现前，若有）**：计划、依赖假设、将触碰的文件/模块。  
3. **Assistant（实现后 / 本轮结论）**：实际结果、差异、验证步骤、已知限制。  
4. **关联**（可选）：路径、PR、REQ 占位等。

## 语言

- 日志正文可用**简体中文**。  
- 产品 **UI 文案**在代码/PRD 中仍须 **English**（与 `.cursorrules` 一致）。

## 与正式 PRD 的关系

- **本日志** = 全量溯源；**正式 PRD** = 结构化定稿。  
- 仅**涉产品需求**的稳定条目可周期性合并进 PRD 的 REQ；合并后可于该条「关联」回填 `REQ-x.y-nn`。
