# Vouchap Workspace 全局 AI 指南

- **vouchap-app**：主应用（移动端 + Web），位于 `./vouchap-app`。逻辑分层：`src/shared-logic`（API、收据解析等共用）、`src/mobile-ui`（移动端 UI）、`src/web-ui`（Web UI）。运行 Web：在 `vouchap-app` 下执行 `npm run web`。
- **vouchap-website**、**vouchap-crm**：保留在本 Workspace 中，与 vouchap-app 并列。
- **Project-map**：原 vouchap-web，已更名为 Project-map（目录位于 /Users/macbook/Project-map），包含空间与成员配置及 Mind Map 等，已拷贝所需共享逻辑实现单独自洽。请从本多根 Workspace 中移除「vouchap-web」根目录（若仍存在），Project-map 可单独打开使用。
- 产品的UI文案必须全英文，避免翻译错误。
## 版本管理强制准则（vouchap-app）

项目包含原生 `ios/` 和 `android/` 目录。每当用户要求「更新版本」或「准备构建」时，必须同步修改以下文件（版本号与构建号一致）：

- `vouchap-app/package.json`（version）
- `vouchap-app/package-lock.json`（根 version 与 `packages."".version`）
- `vouchap-app/app.json`（ios.buildNumber、android.versionCode）
- `vouchap-app/app.config.js`（ios.buildNumber、android.versionCode）
- `vouchap-app/android/app/build.gradle`（versionCode、versionName）
- `vouchap-app/ios/*/Info.plist`（CFBundleVersion、CFBundleShortVersionString）

修改完成后自动执行 commit + push，提交信息格式：`发布版本号 X.X.X (Build X)`（X.X.X 为版本号，Build 后的 X 为 iOS CFBundleVersion / Android versionCode）。

## 模型调用规则
- `Always use the full capabilities of the selected model. Do not simplify logic or skip file structures for speed. I am okay with slow responses as long as the code is architecturally sound.